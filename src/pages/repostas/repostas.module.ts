import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { RepostasPage } from './repostas';

@NgModule({
  declarations: [
    RepostasPage,
  ],
  imports: [
    IonicPageModule.forChild(RepostasPage),
  ],
})
export class RepostasPageModule {}
